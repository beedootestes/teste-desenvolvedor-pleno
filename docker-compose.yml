version: "3"
services:
    mongo:
        container_name: mongo-container
        image: mongo
        restart: always
        volumes:
            - ./data:/data/db
        environment:
          - MONGO_INITDB_ROOT_USERNAME=jobserver
          - MONGO_INITDB_ROOT_PASSWORD=beedoo
        ports:
            - "27017:27017"